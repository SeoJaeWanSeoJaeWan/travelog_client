import styled from "styled-components";

const Logo = styled.img`
  position: absolute;
  top: 20px;
  right: 20px;
  z-index: 2;

  width: 100px;
`;

const LogoStyle = {
  Logo,
};

export default LogoStyle;
